TLS: transport layer security; a cryptographic protocol to provide security over a computer network
<br>
widely use, but the most obvious one is securing HTTPS (hypertext transfer protocol secure)
<br>
how it works is basically like how we setup ssh on our server + personal computer; so it exchange keys and share session.
this prevent attack from outside and man in the middle (since its encrypted with parties keys)
<br>
after asking gpt for knowledge confirmation: TLS = Authentication (CA) + Key Exchange + Symmetric Encryption
so we have to authenticate first with CA (Certificate Authorities)


